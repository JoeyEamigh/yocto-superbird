# kas-managed upstream layer clones live under sources/
/sources/

# kas internal state
/.kas/

# local-only kas overlay for externalsrc against an unpushed bridgething
# working tree — see kas/dev-local.yml.example
/kas/dev-local.yml

# bitbake build dir + ccache + flash artifact staging
/build/
/ccache/
/artifacts/

# editor / OS
.DS_Store
*.swp
