# libubootenv config: u-boot env is the uboot.env file inside the env-partlabel vfat
# mounted at /mnt/uboot-env; libubootenv can't parse FAT directly so it reads the file.
# format: <device>  <offset>  <env_size>; single line = non-redundant.
/mnt/uboot-env/uboot.env  0x0000  0x10000
